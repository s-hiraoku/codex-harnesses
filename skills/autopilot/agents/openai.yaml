interface:
  display_name: "PR Autopilot"
  short_description: "Keep a pull request moving until it is merge-ready"
  default_prompt: "Use $autopilot to keep the current pull request moving until it is merge-ready, but do not merge it."
policy:
  allow_implicit_invocation: false
