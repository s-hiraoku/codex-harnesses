# Strict Repository Example

This example shows a conservative harness layout for important repositories.

## Contents

- `AGENTS.md`: strict repository guidance
- `policies/strict.yaml`: policy example for conservative approval, verification, and git behavior

Use this shape for repositories where unsafe edits, skipped checks, or destructive git operations would be costly.

